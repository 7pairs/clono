{
  "name": "clono",
  "version": "0.0.1",
  "private": true,
  "bin": {
    "clono": "dist/clono.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "shadow-cljs compile cli",
    "build:release": "shadow-cljs release cli",
    "watch": "shadow-cljs watch cli",
    "test": "shadow-cljs compile test && node target/tests.js",
    "test:watch": "shadow-cljs watch test-watch",
    "prepack": "npm run build:release"
  },
  "engines": {
    "node": "^22.13.0 || ^24.0.0"
  },
  "devDependencies": {
    "shadow-cljs": "3.4.12"
  }
}
