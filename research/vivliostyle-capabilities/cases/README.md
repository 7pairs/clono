# 調査ケース

このディレクトリには、VivliostyleおよびVFMの機能ごとに独立した最小サンプルを置く。

各ケースのディレクトリ名には英語のkebab-caseを使用し、少なくとも次の内容を含める。

- 検証に必要な入力原稿と設定
- 実行コマンド
- 期待するHTML構造または紙面
- 目視確認項目

共通CSSは`../styles/base.css`を使用する。検証対象に固有のCSSは各ケース内へ置き、ほかのケースへ暗黙に影響させない。
HTMLおよびPDFなどの生成物は`../output/`へ出力し、Gitへコミットしない。
