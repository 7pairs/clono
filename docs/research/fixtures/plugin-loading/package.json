{
  "name": "@clono/research-plugin-loading",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "^22.13.0 || ^24.0.0"
  },
  "scripts": {
    "build:loading": "shadow-cljs release loading",
    "test": "shadow-cljs compile test && node target/tests.cjs",
    "verify": "npm test && npm run verify:loading",
    "verify:loading": "npm run build:loading && node target/verify-loading.cjs"
  },
  "devDependencies": {
    "shadow-cljs": "3.4.12"
  }
}
